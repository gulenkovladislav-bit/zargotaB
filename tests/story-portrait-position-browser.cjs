const{chromium}=require('playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});try{const p=await b.newPage({viewport:{width:1600,height:950}});await p.goto('http://127.0.0.1:5176/index.html?lookout-demo=1');await p.getByRole('button',{name:/Редактор/}).click();await p.locator('[data-workspace-tab=scene]').click();await p.locator('.zg-stage-tabs [data-mode=look]').click();await p.getByRole('button',{name:'▣ Предпросмотр диалога',exact:true}).click();const slider=p.getByRole('slider',{name:'Сдвиг влево / вправо',exact:true});await slider.fill('20');await slider.dispatchEvent('change');await p.getByRole('button',{name:'↑ Выше',exact:true}).click();await p.getByLabel('Отразить изображение',{exact:true}).check();const frame=p.locator('.zg-story-dialogue-preview .zg-story-speaker-frame');assert.equal(await frame.evaluate(e=>e.style.translate),'20% -5%');assert.equal(await frame.locator('img').evaluate(e=>e.style.scale),'-1 1');await p.reload();await p.getByRole('button',{name:/Редактор/}).click();const saved=await p.evaluate(()=>zgStoryEditorProject().scene.story.presentation);assert.equal(saved.offsetX,20);assert.equal(saved.offsetY,-5);assert.equal(saved.mirror,true);console.log('Portrait offset, nudge, mirror and reload persistence passed');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
