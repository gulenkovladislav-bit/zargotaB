const{chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});try{const p=await b.newPage({viewport:{width:1400,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5182/index.html?vrotik-prologue');await p.locator('.zg-loader-card').waitFor({state:'hidden',timeout:60000});await p.emulateMedia({reducedMotion:'reduce'});await p.evaluate(()=>{const d=JSON.parse(JSON.stringify(ZargotaStoryPrologue));d.intro={enabled:false};const scene=d.scenes.find(s=>s.id===d.startSceneId).scene;scene.story.cameraZoom=2;scene.story.walkable=null;d.walkable=null;const hero=scene.tokens.find(t=>t.id===d.heroTokenId),brother=scene.tokens.find(t=>t.id!==d.heroTokenId);hero.x=brother.x-2;hero.y=brother.y;scene.story.spawnX=hero.x;scene.story.spawnY=hero.y;d.nodes[0].cameraEvent={enabled:true,target:'point',x:30,y:50,ping:true,durationMs:200,returnToHero:true};ZargotaStoryPlayer.start(d);});await p.getByRole('button',{name:'Брат Вротика · Взаимодействовать',exact:true}).click();await p.locator('.zg-story-camera-ping').waitFor();assert.equal(await p.locator('.zg-story-camera-ping').count(),1);await p.locator('.zg-story-answers button').first().click();await p.locator('.zg-story-camera-ping').waitFor({state:'detached'});await p.getByRole('button',{name:'✎ Редактор',exact:true}).click();await p.locator('[data-workspace-tab="dialogues"]').click();await p.getByRole('button',{name:'События',exact:true}).click();assert(await p.getByLabel('Событие при появлении реплики').isVisible());assert.deepEqual(errors,[]);console.log('Runtime dialogue ping appears, clears on next line; camera event editor is accessible; no runtime errors');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
