const assert=require('node:assert/strict'),vm=require('node:vm'),fs=require('node:fs');let callback,removed=0,draws=[];const world={appendChild(){}};const ctx={window:{},innerWidth:1400,innerHeight:900,performance:{now:()=>0},matchMedia:()=>({matches:false}),requestAnimationFrame:fn=>(callback=fn,1),cancelAnimationFrame:()=>{callback=null},document:{createElement:()=>({style:{},setAttribute(){},remove(){removed++}})}};vm.createContext(ctx);for(const p of ['story-camera.js','story-camera-events.js'])vm.runInContext(fs.readFileSync(p,'utf8'),ctx);const api=ctx.window.ZargotaStoryCameraEvents,scene={boardWidth:24,boardHeight:16,gridSize:64,story:{cameraZoom:2}},hero={x:20,y:50};let stop=api.run({x:80,y:20,ping:true,durationMs:1000},scene,world,hero,{x:0,y:0},c=>draws.push(c));callback(500);assert(draws[0].x<0);callback(1000);const target=api.target(scene,{x:80,y:20});assert.equal(draws[1].x,target.x);assert.equal(draws[1].y,target.y);stop();assert.equal(removed,1);assert.equal(callback,null);draws=[];stop=api.run({x:80,y:20,ping:true,move:false},scene,world,hero,{x:0,y:0},c=>draws.push(c));callback(2000);assert.equal(draws.length,0);stop();assert.equal(removed,2);console.log('Camera events: bounded target, tween completion, cancellation and ping-only passed');
