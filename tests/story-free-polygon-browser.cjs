const{chromium}=require('playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});try{const p=await b.newPage({viewport:{width:1600,height:950}});await p.goto('http://127.0.0.1:5176/index.html?lookout-demo=1');await p.getByRole('button',{name:/Редактор/}).click();await p.locator('[data-workspace-tab=scene]').click();await p.getByRole('button',{name:'⬡ Нарисовать зону',exact:true}).click();const box=await p.locator('.zg-stage-minimap').boundingBox(),points=[[41.3,42.7],[56.7,43.2],[48.1,61.7]];for(const[x,y]of points)await p.mouse.click(box.x+box.width*x/100,box.y+box.height*y/100);await p.getByRole('button',{name:'✓ Завершить контур',exact:true}).click();const saved=await p.evaluate(()=>zgStoryEditorProject().scene.story.zones.at(-1).points);for(let i=0;i<3;i++)for(let j=0;j<2;j++)assert.ok(Math.abs(points[i][j]-saved[i][j])<.15,JSON.stringify(saved));console.log('Polygon vertices follow cursor without grid snapping');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
