const{chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});try{const p=await b.newPage({viewport:{width:1400,height:900}});await p.goto('http://127.0.0.1:5182/index.html?vrotik-prologue');await p.locator('.zg-loader-card').waitFor({state:'hidden',timeout:60000});await p.getByRole('button',{name:'✎ Редактор',exact:true}).click();await p.getByRole('button',{name:'☷ Панель героя',exact:true}).click();await p.getByRole('button',{name:'Угол панели: ↘',exact:true}).click();const preview=await p.locator('.zg-hud-layout-editor .zg-story-health').boundingBox();await p.getByRole('button',{name:'Сохранить',exact:true}).click();await p.getByRole('button',{name:'▶ Пройти эпизод',exact:true}).click();const actual=await p.locator('#zg-story-player .zg-story-health').boundingBox();for(const k of ['x','y','width','height'])assert(Math.abs(preview[k]-actual[k])<1,`${k}: ${preview[k]} vs ${actual[k]}`);await p.getByRole('button',{name:'✎ Редактор',exact:true}).click();await p.getByRole('button',{name:'☷ Панель героя',exact:true}).click();await p.getByRole('button',{name:'Угол панели: ↖',exact:true}).click();await p.getByRole('button',{name:'Отмена',exact:true}).click();await p.getByRole('button',{name:'▶ Пройти эпизод',exact:true}).click();const after=await p.locator('#zg-story-player .zg-story-health').boundingBox();assert.equal(after.x,actual.x);assert.equal(after.y,actual.y);console.log('HUD saved position matches preview pixel-for-pixel; cancel preserves saved position');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
