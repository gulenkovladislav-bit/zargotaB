# Заргота · Карта проекта

Разбор структуры и логики всех блоков. Составлено анализом кода, не со слов.
Назначение: чтобы Claude и Codex одинаково понимали, где что лежит и откуда растут баги.

---

## Общая картина

Всё приложение — **один файл** `index.html`, 50 320 строк, 2.59 МБ.
Плюс `zargota-network.js` (1013 строк) — сетевой слой Firebase.

| Что | Объём | Доля |
|---|---|---|
| Скрипты | 2.07 МБ | 80% |
| Стили | 386 КБ | 14% |
| Разметка | ~130 КБ | 5% |
| Картинки внутри файла | 0 | 0% |

Картинки base64 внутри файла отсутствуют — это хорошо, они лежат отдельно
(`images/`, IndexedDB, GitHub).

---

## Страницы

Переключение — через `showPage(name)`, показывает `div#page-<name>`.

| Блок | Строка | HTML | Точка входа при показе |
|---|---|---|---|
| home | 15366 | 160 КБ | `restartHomeParticles()` |
| arena | 16740 | 2.6 КБ | `renderArenaHub()` |
| catalog | 16757 | 6.3 КБ | `renderCatalog()` |
| **chars** | 16861 | 0.4 КБ | **мёртвая** (см. ниже) |
| **detail** | 16872 | 0.9 КБ | **мёртвая** |
| **form** | 16895 | 11.7 КБ | **мёртвая** |
| characters | 17083 | 7.1 КБ | `showCharView()` + `loadZoom()` |
| bestiary | 17213 | 5.1 КБ | `showBeastView()` |
| world | 17310 | 41.5 КБ | `initWorldPage()` |
| manual | 17362 | 0.7 КБ | `renderManualPage()` |
| workshop | 17371 | ~0 | `zgNpcInit()` — Реестр НПС |

Страница `home` тяжёлая (160 КБ), потому что в ней же лежит **весь VTT-стол**:
игровой оверлей, панель мастера, поп-апы, лист персонажа, кубик, инструменты.

---

## Модули (крупные, >150 строк)

| Строка | Строк | Модуль |
|---|---|---|
| 35227 | **9552** | BESTIARY MODULE |
| 17745 | 4083 | WORLD ATLAS EDITOR · карта и объекты |
| 21828 | 3419 | РЕЕСТР НПС (`zargota_npc_v1`) |
| 15272 | 2369 | ZG IMG RESOLVER — перехват ошибок `<img>` |
| 29701 | 2023 | renderCharSheet + хелперы |
| 47426 | 1819 | Бестиарий: памятка по типам существ |
| 10371 | 1499 | СЦЕНА VTT |
| 32715 | 1423 | Семейное древо персонажа |
| 46190 | 1236 | SHOP PAGE — витрина товаров |
| 34142 | 1085 | SPELL REFERENCES — связь персонаж ↔ гримуар |
| 14027 | 1059 | PUBLISH MODAL — публикация в GitHub |
| 31724 | 991 | LEVEL-UP по мануалу |
| 44779 | 978 | SPELL CAST SYSTEM — заклинания на арене |
| 26999 | 673 | QUICK ADD — парсинг через Claude API |
| 27672 | 569 | ПЕРСОНАЖИ — старая система |
| 13469 | 558 | EXPORT data.json |
| 45757 | 433 | MANUAL PAGE |
| 12810 | 371 | ZG DATA LOADER |
| 9143 | 372 | ZARGOTA SOUND ENGINE v2 |

---

## Хранение данных

### localStorage — основные ключи

| Ключ | Что | Блок |
|---|---|---|
| `grimoire_chars` | листы персонажей | Персонажи |
| `grimoire_v3` | каталог заклинаний | Каталог |
| `grimoire_beasts` | бестиарий | Бестиарий |
| `zargota_npc_v1` | реестр НПС | Реестр НПС |
| `zargota_armory_v1` | оружейная | Арена |
| `zargota_shop_v1` | товары | Товары |
| `zargota_status_mechanics_v1` | механики состояний | Состояния |
| `grimoire_world_atlas_v5` | атлас мира | Мир и Лор |
| `grimoire_world_map_source_v1` | картинка карты | Мир и Лор |
| `grimoire_enc_v1` | энциклопедия | Мир и Лор |
| `grimoire_htree_v1` | иерархии | Мир и Лор |
| `zg_data_version` | версия загруженных данных | Загрузчик |

### IndexedDB

| База | Назначение |
|---|---|
| `zargota_chars` | резервная копия листов (обход квоты localStorage) |
| `zargota_beasts` | бестиарий |
| `zargota_armory_db` | оружейная |
| `zargota_media` | картинки |

### Как данные попадают на устройство

1. Страница грузится → `ZG DATA LOADER` тянет `manifest.json` с GitHub.
2. Сравнивает `manifest.version` с `localStorage.zg_data_version`.
3. Если версии разные — качает `data.json` и раскладывает по ключам.
4. Если совпадают — использует локальные данные.

**Публикация в обратную сторону:** `EXPORT data.json` собирает всё в один JSON,
`PUBLISH MODAL` заливает в GitHub. То есть мастер редактирует локально и публикует,
игроки получают при следующей загрузке.

---

## Связи между блоками

Единой шины данных нет. Мост один — геттеры внутри модуля Арены (строка 37649):

```js
function getEntries()    { return global.entries    }  // заклинания из Каталога
function getCharacters() { return global.characters }  // листы Персонажей
function getBeasts()     { return global.beasts     }  // существа Бестиария
```

Фактические зависимости:

- **Арена** читает Каталог (заклинания), Персонажей (бойцы), Бестиарий (монстры) — самый связанный блок.
- **Мир и Лор** ссылается на Бестиарий (привязка существ к локациям) и Реестр НПС.
- **Персонажи** ссылаются на Каталог через `SPELL REFERENCES` (`spellRefs` в листе).
- **VTT-стол** читает `window.characters`; по плану должен читать ещё Бестиарий и Реестр НПС.
- **Состояния** — источник правды по статусам для боя.

---

## Проблемы, найденные в коде

### 1. Две системы персонажей, живая одна

Существуют два глобальных массива:

- `chars` (строка 27676) — **старая система**, наполняется только `_old_loadChars()`,
  которая **никогда не вызывается**. Массив всегда пустой.
- `characters` (строка 28248) — **живая система**, наполняется `loadChars()`.

Мёртвыми остались: страницы `page-chars`, `page-detail`, `page-form` (13 КБ разметки)
и функции `buildCharList`, `buildCharDetail`, `changeHp`, `editStat`, `editStatField`,
`editInitiative`, `resetSpells`, `saveCharNotes`, `openCharForm`, `buildCharForm`,
`submitCharForm`.

В `showPage` мёртвая ветка недостижима из-за порядка строк:

```js
if (name === 'chars') name = 'characters';   // подмена происходит раньше
...
if (name === 'chars') renderCharsPage();     // сюда управление уже не попадает
```

### 2. Импорт JSON пишет в мёртвый массив — ЖИВОЙ БАГ

`importJSON()` (строка 26734) висит на `<input id="import-file">` и делает:

```js
if (data.chars) chars = data.chars;          // пишет в МЁРТВЫЙ массив
```

При этом `saveChars()` сохраняет `characters`. Итог: **импорт персонажей молча
не работает**, без ошибок и уведомлений.

### 3. Дублирующиеся объявления в глобальной области

Функции и переменные объявлены дважды; из-за подъёма объявлений работает последняя,
первая недостижима, но путает при чтении.

| Имя | Строки | Что живёт |
|---|---|---|
| `STATUSES` | 27698, 28265 | вторая |
| `toggleSpellUsed` | 28038, 34405 | вторая (делегирует в `toggleSpellCharge`) |
| `getWorldMapSize` | 17741, 18149 | вторая |
| `_old_toggleStatus` | 27994, 31292 | мёртвая целиком |

### 4. Статусы описаны в четырёх местах

- `STATUSES` ×2 (мёртвая и живая)
- `NPC_STATUSES` (строка 21833) — для реестра НПС
- `ALL_STATUSES` (строка 29707) — 23 статуса в листе персонажа
- `zargota_status_mechanics_v1` — редактируемая база блока «Состояния»

Для боя это узкое место: **система должна брать статусы из одного источника**,
иначе правила разъедутся. Кандидат на источник правды — `zargota_status_mechanics_v1`,
потому что он редактируется из интерфейса и переживает обновления.

### 5. Ловушка «метка сохранена, данные — нет» (исправлено)

Загрузчик решал качать или нет **только по версии**. Запись данных при переполнении
квоты не прерывала процесс, а метка версии сохранялась всё равно — человек навсегда
оставался с пустым списком персонажей.

Исправлено: при совпадении версий дополнительно проверяется фактическое наличие
данных в localStorage и IndexedDB.

**Проверить в других местах:** тот же шаблон может встречаться там, где признак
успеха пишется до подтверждения записи.

### 6. Проверено и признано нормальным

- Частые таймеры (70 и 140 мс) — короткие анимации броска, гасятся сразу. Не утечка.
- Утечек слушателей в render-функциях нет.
- `renderCharSheet` объявлена один раз; «PATCHED» — просто название секции.
- Картинок base64 внутри файла нет.

---

## Сетевой слой (Firebase)

Узлы под `rooms/{code}`:

| Узел | Что лежит | Кто пишет |
|---|---|---|
| `members/{uid}` | uid, role, name, online, lastSeen, characterId, character, gmReady, zone | игрок — только свою запись; мастер — любую |
| `pending/{code}` | заявки на вход по личному коду | входящий игрок |
| `slots` | пять мест партии | мастер |
| `scene` | опубликованная сцена (слои, токены, сетка, камера) | мастер |
| `zones/{zoneId}` | сцены зон для перехода | мастер |
| `cameraCue` | перевод экрана игроков | мастер |
| `liveMeasurements/{uid}` | измерения линейкой в реальном времени | каждый свою |
| `lastRoll` / `activeRoll` | текущий бросок кубика | любой |

Правила доступа (`database.rules.json`): комнату пишет только владелец `masterUid`;
игрок может писать только `members/{свой uid}` и только с `role: player`.

**Заявка на движение:** `id, uid, name, tokenId, fromX, fromY, toX, toY, zoneId, status, createdAt`
**Бросок:** `id, ts, duration, sides, value, total, outcome, statLabel, speakerUid, name`

---

## Схемы сущностей (из реального `data.json`)

### Герой (`characters`, 6 записей)

```
id, name, type, level, race, klasse, age, portrait, biography
hpCur, hpMax, baseHpMax        ac, baseAc        initiative, baseInitiative
speed, baseSpeed               stats {}          statuses[]
spellRefs[], spellCD, spellsLearned
inventory, inventoryItems[], equipItems[], coins
arenaSetId, arenaEquipSlots, mastery
fameMundo, fameZargoty, deathSaves, tempEffects, effectHistory, battleEcho
_updatedAt, _equipBonusCache, _equipAppliedHpBonus
```

Шаблон `base*` → итог: снаряжение прибавляется к базовым значениям, результат
кешируется в `_equipBonusCache`. Бой обязан считать по этому же правилу,
иначе бонусы применятся дважды.

### Зверь (`beasts`, 82 записи)

```
id, name, type, family, role, size, level, threat, alignment
hp, ac, initiative, speed
str, dex, con, int, wis, cha
attacks[], traits[], resistances, immunities, vulnerabilities
shortActionsPerTurn, longActionsPerTurn, reactionsPerRound
loot, lore, behavior, description, variants[]
portrait, portraitFocusX, portraitFocusY
arenaSetId, arenaEquipSlots
```

У зверей уже есть **бюджет действий за ход** — короткие, длинные, реакции.
Это готовая основа для боевой системы.

### Состав `data.json` (983 КБ)

| Ключ | Вес |
|---|---|
| catalog | 230 КБ |
| atlas | 173 КБ |
| beasts | 147 КБ |
| encyclopedia | 80 КБ |
| htree | 28 КБ |
| characters | 20 КБ |
| остальное | < 10 КБ каждое |

---

## Три конфликта, критичных для боя

### A. Характеристики героев и зверей не совпадают

- Герой: `str, dex, int, cha, **per**, con` — шесть по канону Зарготы, есть Восприятие.
- Зверь: `str, dex, con, int, **wis**, cha` — классическая шестёрка D&D, есть Мудрость.

**Пересечение неполное.** У героя нет `wis`, у зверя нет `per`. Любая формула,
которая сравнивает характеристику атакующего и защищающегося, сломается на этой паре.

Решать **до** написания боя: либо привести зверей к канону Зарготы (`wis` → `per`),
либо завести явную таблицу соответствия.

### B. Реестр НПС не публикуется игрокам

Ключа `npcs` в `data.json` **нет**. Реестр живёт только в `localStorage.zargota_npc_v1`
на устройстве мастера. Значит токены, привязанные к НПС, у игроков останутся без данных.

Нужно либо добавить НПС в экспорт, либо передавать нужный минимум через Firebase
вместе со сценой.

### C. Механики состояний не публикуются

`statusMechanics` в `data.json` **пуст**. 23 статуса с механиками существуют только
в `localStorage.zargota_status_mechanics_v1` у мастера.

Если бой будет считать эффекты на стороне игрока — у него не будет правил.
Варианты: публиковать механики в `data.json`, либо считать эффекты только у мастера
и рассылать результат.

---

## Что стоит сделать по итогам

| Приоритет | Действие | Риск |
|---|---|---|
| **Блокирует бой** | Согласовать характеристики героев и зверей (`per` ↔ `wis`) | Средний, но чем позже — тем дороже |
| **Блокирует бой** | Решить, как игроки получают механики состояний | Средний |
| **Блокирует токены НПС** | Добавить реестр НПС в публикацию | Низкий |
| Высокий | Починить `importJSON` — писать в `characters` | Низкий, одна строка |
| Высокий | Свести четыре набора статусов к одному источнику | Средний |
| Средний | Удалить мёртвую систему персонажей (3 страницы + 11 функций + массив) | Нужно подтверждение |
| Средний | Убрать дубли `STATUSES`, `getWorldMapSize`, `_old_toggleStatus` | Низкий |
| Низкий | Разбить `BESTIARY MODULE` (9552 строки) на части | Долго |

---

## Производительность: замеры

### Картинки — главный вес проекта

| Что | Значение |
|---|---|
| Всего | **352 МБ**, 197 файлов |
| Средний файл | 1831 КБ |
| Медиана | 2064 КБ |
| Тяжелее 1 МБ | 115 файлов = 340 МБ (**96% всего веса**) |
| Формат | 149 PNG, 44 JPG |

По папкам: `beasts` 264 МБ, `npcs` 54 МБ, `portraits` 11 МБ, `heroart` 9.4 МБ,
`map.jpg` 7.4 МБ одним файлом.

Для сравнения: весь код проекта — 2.6 МБ. **Картинки тяжелее кода в 135 раз.**

Портрет зверя по 3–4 МБ — это портрет, который показывается в карточке размером
с ладонь. Полтораста PNG там, где почти везде подошёл бы WebP.

### Отрисовка

- `innerHTML =` — **253 места**. Это полная замена поддерева: браузер выбрасывает
  все узлы и строит заново. Для списков из десятков элементов приемлемо,
  для сцены в бою — нет.
- `renderTokens()` создаёт **до 10 узлов на жетон** (картинка, подпись, HP-бар,
  рамка, ручка, панель действий) и вызывается из **10 мест**.
- `applyScene()` перерисовывает **всё сразу**: фоны, регионы, жетоны. Вызывается
  из `markDirty()`, то есть **на каждое изменение** — включая каждый шаг
  перетаскивания ползунка.

### Замер пережатия: проверено на реальных файлах

Двенадцать самых тяжёлых файлов пересчитаны в WebP качества 82 с ограничением
длинной стороны до 1600 px:

| Файл | Сейчас | WebP q82 | Экономия |
|---|---|---|---|
| map.jpg | 7541 КБ | 203 КБ | **98%** |
| beast_1775852776698.png | 4032 КБ | 527 КБ | 87% |
| beast_1775850301096.png | 4020 КБ | 487 КБ | 88% |
| остальные 9 зверей | ~3.5 МБ каждый | ~430 КБ | 88–90% |
| **Итого по 12 файлам** | **46.6 МБ** | **5.0 МБ** | **90%** |

Если экономия сохранится на всём наборе, **352 МБ превращаются примерно в 35 МБ**.

Карта мира — отдельный случай: 7.5 МБ JPEG ужимается до 203 КБ, то есть в 37 раз.
Скорее всего она сохранена без сжатия.

Оговорка: замер сделан на 12 файлах из 197. Экономия на мелких файлах будет ниже.
Ограничение 1600 px подходит для портретов, но **для больших фонов зон может быть
мало** — заказчик отмечал, что там качество принципиально.

### Вывод по производительности

Узкое место — **не код, а картинки**. 352 МБ означают, что игрок при первом заходе
тянет сотни мегабайт, а GitHub Pages отдаёт их без сжатия. Пережатие — самая
дешёвая и самая крупная оптимизация во всём проекте: одна разовая операция,
десятикратное сокращение.

Вторая по значимости — полная перерисовка сцены на каждое изменение. Пока жетонов
единицы, это незаметно. В бою с двумя десятками участников и анимациями — станет заметно.

### CSS — проблем нет

389 КБ, 3 блока `<style>`, 3084 правила, 58 медиазапросов, 182 `!important`.
Повторяющиеся селекторы (`0%`, `100%`, `from`, `to`) — это ключевые кадры анимаций,
а не дубли. Чистить нечего.

### Поправка к таблице модулей

Размеры модулей выше определены по комментариям-заголовкам и **для нескольких
позиций завышены**: в диапазон попадала разметка страниц, а не только код.
Проверенный пример: «ZG IMG RESOLVER, 2369 строк» — на деле в этом диапазоне
591 HTML-тег и всего 2 функции, сам модуль небольшой.

Таблицу стоит воспринимать как ориентир «где что лежит», а не как точный замер объёма.

---

## Боевая система: она уже существует

Самая важная находка разбора. Диапазон 35227–44778 (498 КБ, 378 функций) назван
«BESTIARY MODULE», но на деле там **и Бестиарий, и полноценный боевой движок Арены**.

### Что уже написано

**Модель боя** — объект `battle` с полями:

```
id, name, status, round, turnIndex, currentCombatantId
combatants[], initiative[], grid
pendingMovementIntents[], pendingReactionPrompts[]
log[], resultSummary, updatedAt, finishedAt
```

**Готовые механизмы:**

| Область | Функции |
|---|---|
| Ход и раунд | `advanceTurn`, `advanceRound`, `advanceTime`, `advanceGlobalTime` |
| Инициатива | `buildInitiative`, `sortInitiativeEntries`, `initiativeRibbon`, `initiativePanel`, `initiativeOrderHtml`, `openInitiativePreview`, `zgNpcAutoInitiative` |
| Атаки | `resolveAttackRoll`, `performBasicAttack`, `performManualAction`, `rollDamage`, `applyDamage` |
| Заклинания | `resolveSpellAction`, `resolveSpellActionDirect`, SPELL CAST SYSTEM (978 строк) |
| Реакции | `performReaction`, `resolveReactionPrompt`, `dismissReactionPrompt`, `pendingReactionPrompts` |
| Статусы | `applyStatus`, `applyStatusesToCombatant`, `applyStatusMechanicsEndOfTurn`, `applyCustomStatus` |
| Спасброски | `rollSavingThrow`, `rollFormula`, `rollDice`, `rollD` |
| Снаряжение | `applyCharacterEquipmentBonuses`, `calcArenaEquipBonus`, `calcEquipmentAcBonus`, `calcDexAcBonusFromScore`, `calcSetStats` |

**Глубина проработки** — по коду `performReaction` видно, что учитываются: активность боя,
состояние «при смерти» у реактора и цели, запрет реакции на себя, запрет реакции
для опрокинутого. То есть это не заготовка, а работающая система с проверками правил.

Счётчики по всему файлу: `damage` — 362 упоминания, `round` — 211, `reaction` — 105,
`initiative` — 70, `longAction` — 37, `shortAction` — 30.

### Что это значит для плана

Заказчик описывал бой как то, что предстоит написать: инициатива, очередь, долгое
и короткое действие, реакция вне своего хода, урон со снаряжением, логирование.
**Почти всё это уже реализовано в Арене.**

Задача на самом деле другая: не «написать бой», а **вывести существующий бой на стол** —
подключить к жетонам на сцене, к сетевому слою и к интерфейсу VTT.

Это принципиально дешевле и принципиально безопаснее, чем писать заново.
Заодно снимает риск, что на столе и на арене правила разойдутся.

### Что проверить перед подключением

- `battle.grid` уже есть — как он соотносится с сеткой сцены VTT?
- `pendingMovementIntents` в бою и `requestMovement` в сети — это два разных механизма
  заявок на движение. Их надо свести, иначе будет два несовместимых пути.
- Бой хранится локально у мастера; для игроков понадобится трансляция состояния.

---

## Карта мира: «врата» уже наполовину готовы

Вторая крупная находка. Заказчик описывал карту-врата как то, что предстоит сделать
с нуля: отдельный редактор, рисование областей поверх картинки, привязка сцен,
наведение курсором, переход по одобрению.

**Редактор карты мира уже умеет почти всё это.**

### Что есть в `atlas`

```
atlas = { version, layers[], objects[], factions[] }
```

**Слои** — полноценная система, пять штук из коробки:

| id | Название | Цвет |
|---|---|---|
| zones | Зоны | #cfa35a |
| routes | Маршруты | #d8c178 |
| markers | Маркеры | #e6bf72 |
| hidden | **Скрытые триггеры** | #8d74d4 |
| hazards | Опасности | #c55e5e |

У слоя есть `visible`, `locked`, `order` — то есть видимость, блокировка и порядок.

**Объекты** — 53 штуки, из них 48 зон и 5 маршрутов:

```
id, name, type, layerId, priority, visible, locked
geometry { kind: polygon | line, points[] }
style    { stroke, fill, strokeWidth, opacity }
behavior { hoverActions[], clickActions[] }
meta     { description, ... }
```

### Почему это важно

1. **Полигоны уже рисуются.** 48 зон нарисовано вручную поверх карты — это ровно
   «рисую зоны на картинке-карте» из задания.
2. **Поведение уже заложено.** `behavior.hoverActions = ["tooltip","highlight"]`
   и `clickActions = ["panel"]` — механизм наведения и клика существует.
   Заказчик хотел: навёлся — увидел подпись, нажал — заявка. Половина есть.
3. **Есть слой «Скрытые триггеры».** Он уже задуман под то, чего заказчик хочет
   для скрытых меток.
4. **Есть `meta.description`** — то есть текст, привязанный к области. Это близко
   к «шпаргалке зоны».

### Чего не хватает для задания

- **Связи «область → сцена VTT»**. В `meta` нет поля вроде `sceneId`.
- **Заявки на переход**: `clickActions` открывает панель, но не отправляет запрос мастеру.
- **Переноса токенов** после одобрения.
- Отдельного **режима просмотра для игрока** (сейчас карта — инструмент мастера).

То есть работа не «сделать карту-врата», а **достроить три недостающих звена
поверх готового редактора**: поле связи со сценой, заявка мастеру, перенос токенов.

---

## Реестр НПС: готовый мост к токенам

Третья находка того же рода. Реестр — не список карточек, а **система автоматического
расчёта персонажей по шкале уровней Зарготы**.

### Шкала уровней (`ZG_NPC_LEVEL_SCALE`)

Одиннадцать ступеней, ровно по канону заказчика:

| Ур. | Кто это | Бюджет статов | Потолок стата |
|---|---|---|---|
| 1 | Новички | 4 | 2 |
| 2 | Обученные | 7 | 3 |
| 3 | Специалисты | 10 | 4 |
| 4 | Профессионалы | 13 | 4 |
| 5 | Элитные специалисты | 16 | 5 |
| 6 | Элита острова | 19 | 5 |
| 7 | Фактор региона | 22 | 6 |
| 8 | Почти легенды | 25 | 6 |
| 9 | Аномальные личности | 28 | 7 |
| 10 | Живые катастрофы | 32 | 8 |
| 11 | Абсолют | 38 | 10 |

Боссу бюджет увеличивается на 2.

### Ключевая функция: `zgNpcResolvedSheet(npc)`

Принимает запись реестра и **возвращает готовый лист**:

```
level, levelLabel, levelCompat, race, age, gender, build, ageStage, archetype,
hp, armor, init, threat, stats, appearance, behavior, about, secret
```

Пустые поля достраиваются автоматически: `zgNpcInferRace`, `zgNpcInferLevel`,
`zgNpcInferGender`, `zgNpcInferBuild` выводят их из имени, роли, места, фракции,
тегов и описания. Дальше `zgNpcDistributeStats` раскидывает бюджет по характеристикам
с учётом жизненного контекста, а `zgNpcCalcHp`, `zgNpcAutoArmor`, `zgNpcAutoInitiative`
считают производные.

### Почему это важно для двух задач заказчика

**1. Токены из реестра.** `zgNpcResolvedSheet()` отдаёт ровно тот набор, который нужен
жетону на столе: имя, портрет, уровень, HP, броня, инициатива, характеристики.
Это готовая точка подключения — писать преобразование не нужно.

**2. Две версии информации.** Заказчик хотел «инфа при встрече» и «расширенная».
В реестре уже есть разделение по смыслу:

| Поле | Кому подходит |
|---|---|
| `appearance` — внешность | видно игрокам при встрече |
| `behavior` — поведение | видно игрокам при встрече |
| `about` — про него | расширенная |
| `stats`, `hp`, `armor` | расширенная |
| `secret` — **секрет ГМа** | **только мастеру, никогда игрокам** |

То есть поля под двухуровневую выдачу уже существуют, нужен только переключатель
в интерфейсе токена и правило, что `secret` не уходит в сеть никогда.

### Ограничение

Реестр живёт только в `localStorage.zargota_npc_v1` и **не попадает в `data.json`**
(см. конфликт B выше). Пока это не решено, токены из реестра у игроков будут пустыми.

---

## Итог разбора: что уже существует

Сводка по всем находкам. Колонка «готовность» — моя оценка, не измерение.

| Задача из плана | Что уже есть | Готовность |
|---|---|---|
| Боевой режим сессии | **см. раздел ниже — оценка «70%» была ошибочной** | правила ~60%, сам режим ~0% |
| Карта-врата | 48 зон-полигонов, слои, `hoverActions`/`clickActions`, `meta.description` | ~60% |
| Токены из Бестиария/НПС | `zgNpcResolvedSheet()` отдаёт готовый лист; у зверей полная схема | ~60% |
| Две версии инфы у токена | Поля `appearance`, `behavior`, `about`, `secret` | ~50% |
| Шпаргалка зоны | `meta.description` у объектов карты | ~20% |
| Туман войны | Система «Обзор» на полигонах | есть, названа заказчиком «кривой» |
| Заявки игроков | `requestMovement`/`resolveMovement` в сети, `pendingMovementIntents` в бою | ~40%, но **два несовместимых механизма** |
| Живые объекты, частицы | — | 0% |
| Экспорт зон файлом | — | 0% |

---

## Три разных боя — не путать (исправление ошибки анализа)

**Ошибка Claude, исправлена по замечанию заказчика.** Ранее в этом журнале бой Арены
был описан как «боевой режим готов на 70%». Это неверно и опасно: если отдать такую
формулировку Кодексу, он решит, что сессионный бой почти есть, и начнёт достраивать
не то.

На сайте **три разных боя с разными целями**:

| | Арена | Бой в блоке «Герои» | **Сессионный бой** |
|---|---|---|---|
| Цель | симуляция для настройки баланса | проверка/тренировка героя | **проведение живой игры ДнД** |
| Кто участвует | расчётные болванки | герой и противники | реальные игроки за своими экранами |
| Кто решает | расчёт | локально | **мастер, по заявке игрока** |
| Сеть | нет | нет | **обязательна, Firebase** |
| Стол, токены | нет | нет | **да, сцена с жетонами** |
| Статус | работает | работает | **не начат** |

Проверка функций Арены подтверждает: `battleColumns`, `battleDetail`,
`battleSetupPanel`, `battleStatusMeta`, `battleSummaryPanel`, `battlefieldPanel` —
это **интерфейс Арены под задачу Арены**. Сетевых заявок там нет вовсе;
`requestMovement` / `resolveMovement` / `pendingMovementIntents` живут отдельно,
в сессионном слое.

### Что реально переиспользуется

Делится **не режим, а правила**. Общее у всех трёх — слой расчёта:

- бросок атаки и попадание;
- расчёт урона с учётом снаряжения, характеристик, прокачки;
- статусы из блока «Состояния» и их применение;
- модификаторы и бонусы.

Это и есть те ~60%: **правила описаны и работают**, их не надо выдумывать заново —
ровно как заказчик и требовал, «вытащить один раз, а не придумывать».

### Что надо писать с нуля

Всё, что отличает живую игру от расчёта:

- очередь хода с портретами вверху и подсветкой активного;
- заявка игрока → карточка мастеру → одобрение → исполнение;
- синхронизация состояния боя между всеми экранами;
- движение по столу в пределах скорости;
- видимость: кто что знает о чужих HP и статусах;
- лог боя, общий и мастерский.

**Вывод для Кодекса:** правила — брать. Режим — писать. Не пытаться приспособить
интерфейс Арены под стол, у него другая задача.

### Почему это самое важное место проекта

Слова заказчика: блок сессий и редактора — **фактически финал сайта**. Всё
остальное (Бестиарий, Реестр, Мануал, Атлас, Арена) — это подготовка материала.
Сессия — то, ради чего материал существует.

---

## Картинки: ловушка при оптимизации

Замеры показали 352 МБ картинок и потенциал сжатия в WebP около 90%
(тест: 46,6 МБ → 5,0 МБ). Но прежде чем жать, проверено, **как на них ссылаются**:

| Где | Сколько жёстких путей |
|---|---|
| `data.json` | 84 ссылки на `.png`, 1 на `.jpg` |
| `index.html` | 74 пути вида `images/<папка>/<файл>.png` |
| **Итого** | **~158 мест** |

Пример пути: `images/beasts/beast_1775678137839.4075.png`

**Вывод: смена расширения на `.webp` оборвёт все 158 ссылок разом** — картинки
исчезнут по всему сайту. Это классический случай «оптимизировали вес, сломали
наполнение». Слово `webp` в коде встречается 5 раз, то есть частичная поддержка
есть, но универсального резолвера расширений нет.

### Два пути

| | Безопасный | Максимальный |
|---|---|---|
| Что делаем | пережимаем на месте, имена и расширения не меняются | конвертируем в WebP + автозамена всех 158 ссылок |
| Экономия | ~50–60% | ~90% |
| Правки в коде и данных | нет | 158 мест |
| Риск | нулевой | требует проверки каждой страницы |

**Рекомендация: начать с безопасного.** Половина веса уходит без единого риска.
Максимальный вариант делается отдельным заходом позже, когда есть время проверить.

### Что при этом НЕ трогается

Ни в одном из вариантов не меняется наполнение: персонажи, Реестр НПС, Бестиарий,
Мануал, Атлас, лор, тексты. Меняется **только вес файлов картинок**, не их
содержимое и не данные.

---

## Влияние работ на визуал, функции и наполнение

Таблица к вопросу «а всё это отобразится на визуале?». Нужна перед каждым заходом.

| Работа | Визуал | Функции | Наполнение |
|---|---|---|---|
| Обновление `PLAN.md` | — | — | — |
| Починка `importJSON` | — | заработает то, что молча не работало | сохраннее |
| Сжатие картинок (безопасное) | на глаз не отличить | — | — |
| Сжатие картинок (WebP) | риск: 158 ссылок | требует правки ссылок | — |
| Удаление мёртвой системы персонажей | уберутся 3 страницы | −11 функций | — |
| Сведение 4 статусов в один | — | да, поведение статусов | требует сверки |
| Публикация НПС в `data.json` | — | токены из реестра заработают | + данные игрокам |

**Правило:** мёртвая система персонажей — единственное, что реально удаляет
страницы. Они сейчас недостижимы из интерфейса, но это удаление и оно делается
**только по прямому подтверждению заказчика**.

---

## Мнение Claude (не проверено с заказчиком)

> Ниже — **моя оценка как Claude**, а не установленный факт и не согласованное решение.
> Заказчик этого не видел и мог бы решить иначе. Помечено отдельно, чтобы не спутать
> с результатами анализа кода выше.

### Главный вывод разбора

Два раза подряд оказалось, что задача, описанная как «надо написать», **уже написана
на две трети**: боевой движок на Арене и рисование зон на карте мира.

Из этого я делаю вывод, который считаю самым практичным во всём документе:
**в этом проекте дешевле сначала искать, чем строить.** Не из экономии, а потому что
альтернатива — третья параллельная система рядом с двумя существующими,
как уже вышло с персонажами и статусами.

Конкретно я бы применил это правило так: прежде чем браться за любой пункт плана,
двадцать минут искать в коде по ключевым словам задачи. По моей оценке это окупается
почти всегда.

### Что я считаю главным риском

Не баги. Главный риск — **файл на 50 тысяч строк, который правят двое вслепую**.
Два массива персонажей, четыре набора статусов, три системы сцен появились не от
небрежности, а потому что предыдущая реализация физически не находится в таком объёме.
Пока это не решено, каждая новая фича будет с некоторой вероятностью написана дважды.

Я бы не стал предлагать «разбить файл на модули» — это дорого и рискованно на живом
проекте. Достаточно дешёвого правила: **перед добавлением сущности искать по имени,
нет ли уже такой**, и вести этот документ как единый список того, что уже существует.

### Про картинки

352 МБ — то, что я бы чинил раньше боя и раньше тумана. Причина: это единственная
проблема, которая бьёт по **каждому игроку на каждом заходе**, а не по мастеру
в редких сценариях.

Замер подтвердил догадку и превзошёл её: не 5–10 раз, а **около 90%** на тяжёлых
файлах. Это разовая операция, которая не требует трогать код вообще.

Как бы я это сделал: пережать всё, кроме больших фонов зон, ограничив сторону
1600 px; фоны зон обработать отдельно с большим лимитом и выше качеством.
Оригиналы сохранить вне репозитория, чтобы можно было вернуться.

Оговорка остаётся: я не знаю, откуда берутся файлы и есть ли у заказчика оригиналы.
Если оригиналов нет — пережимать надо осторожнее и с копией.

### Про бой

Я изменил мнение по ходу разбора и считаю нужным это отметить.

Сначала я предлагал не начинать бой, пока не решены три конфликта. Теперь, увидев,
что **боевой движок уже написан и работает на Арене**, я бы формулировал иначе:
не «писать бой», а «вывести существующий бой на стол».

Это меняет и оценку рисков. Конфликт характеристик (`per` у героев против `wis`
у зверей) существующая Арена как-то уже переживает — значит там есть либо
преобразование, либо она просто не сравнивает эти поля. **Стоит сначала посмотреть,
как это решено на Арене**, и повторить, а не изобретать.

Мой совет Codex, если он возьмётся за бой: прежде чем писать строку кода, прочитать
`performReaction`, `resolveAttackRoll` и `applyStatusesToCombatant`. Есть заметный
шанс, что половина задачи уже сделана.

### Про то, чего я не знаю

- Не измерял реальные кадры в браузере — только структуру. Возможно, всё летает,
  и мои опасения про перерисовку преждевременны.
- Не знаю, какие блоки заказчик реально использует в игре, а какие остались
  от прошлых версий. Мёртвый код я нашёл по коду, но «редко используемое»
  от «мёртвого» статикой не отличить.
- Не знаю планов Codex. Если он уже переписывает что-то из перечисленного,
  часть выводов устареет.

---

## Заметки на будущее

- Файл на 50 тысяч строк редактируют два агента параллельно. Дубли систем
  (персонажи, статусы) появились именно так. Нужны **границы ответственности**
  и общий словарь имён.
- Перед добавлением новой сущности стоит проверять, нет ли уже такой:
  четыре набора статусов и два массива персонажей появились не по злому умыслу,
  а потому что предыдущая реализация была не найдена.
